{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch (g++)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/clocks",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "gdb", 
            "preLaunchTask": "build"
        }
    ]
}
